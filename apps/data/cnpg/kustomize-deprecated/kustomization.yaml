apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Application resources only
# The CNPG operator is deployed separately in apps/platform/cnpg-operator/

resources:
  - namespaces.yaml
  - cluster.yaml
  - backup-pvc.yaml
  - backup-cronjob.yaml

# Note: Secrets are not included by default
# Create your own secret or sealedsecret based on the examples:
# - secret-app.example.yaml (plain secret to encrypt)
# - sealedsecret-app.example.yaml (encrypted secret template)
