apiVersion: v1
kind: Secret
metadata:
  name: platform-postgres-app
  labels:
    app.kubernetes.io/name: platform-postgres
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  username: platform
  password: CHANGE_ME_STRONG_PASSWORD_HERE
  # Example: Use a strong password generator
  # openssl rand -base64 32
